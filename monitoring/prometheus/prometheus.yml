global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "forge_backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["localhost:8000"]
        labels:
          service: "forge-api"
          env: "development"
