apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Grafana runs in Docker and reaches Prometheus over the Compose network.
    url: http://prometheus:9090
    isDefault: true
    editable: false
